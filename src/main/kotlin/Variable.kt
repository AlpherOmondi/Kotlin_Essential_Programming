fun main(){
    // val keyword is used for defining immutable variables.
    val ourFirstVariable: Boolean = true
    println(ourFirstVariable)
    var isItComplex: Boolean = false

    val levelOfMastery = "expert"
    if (levelOfMastery == "newbie") {
        isItComplex = true
    }
}